# 验证记录

日期：2026-09-22

## v1.0.0 合并发布验证

- 正式仓库继续使用 `kunlun7210/lama-watermark-web` 和原 GitHub Pages 地址；运行时代码基于 `lama-watermark-web-next@82a737f`，并保留仓库 1 的模型分片、模型缓存键、IndexedDB 数据库与旧入口迁移桥。
- 页面标题、浏览器标题和添加到主屏幕标题均为“Xiaolin 去水印”；生产预览显示 `v1.0.0 · 2026.09.22`。
- `npm test` 全部通过：实际模型分片尺寸和完整 SHA-256、Gemini 误判门槛、Worker 初始化/超时、OOM 4 → 2 → 1 重建、IndexedDB 结果策略、预计时间、ZIP、Vite 生产构建和发布资产均纳入检查。
- Chrome 生产预览以 402 × 874、3x、iPhone Safari UA 验证 UI 和跨源隔离；批次上限、损坏单图恢复、未识别原图逐字节保持及三代旧入口迁移均通过。
- 20 张真实图片在第 3 张处理中刷新：刷新前 2 个结果和全部 20 个原图恢复，最终 17 张修复、3 张保持原图、0 失败、控制台错误 0，用时 115.7 秒。
- 完成后再次刷新恢复 20/20 个结果，等待 3 秒模型请求为 0；完整结果按需从 IndexedDB 读取，不在页面状态中常驻。
- ZIP 含 20 个 UTF-8 文件条目，解压后共 70,355,305 bytes；`unzip -t` 与 Python `ZipFile.testzip()` 均通过，CRC 错误为 0。
- Playwright WebKit 26.6 完成一张真实图片推理，Worker、SharedArrayBuffer 和跨源隔离均正常；该自动化引擎提示结果未落盘。随后用本机原生 Safari 跑同一生产预览，Gemini 图片 10.2 秒完成且无“结果未落盘”，刷新后恢复 1/1 个结果。两条结果分别保留，避免把自动化 WebKit 等同于真实 Safari。

## 自动检查

- `npm test`：模型清单固定值、实际二进制尺寸与完整哈希、Gemini 门槛、Worker 初始化生命周期、OOM 动态重试、任务上限策略、ZIP 格式、Vite 生产构建和稳定入口文件检查全部通过。
- OOM 动态重试检查：实际模拟前两次推理抛出 WASM OOM，调用顺序为 4 → 2 → 1，第三次成功；普通 `RangeError` 和 `no available backend` 均直接报错，不触发错误降级。
- Worker 生命周期检查：正常 ready、初始化报错、初始化超时和外部重建四条路径均已覆盖；失败与超时都会终止 Worker，初始化 Promise 不再悬空。
- GitHub Actions 在构建后启动真实 Chrome，执行 iPhone 17 Pro 视口、版本格式、页面隔离、旧线程偏好清理、关键 UI 文案和四项审计边界回归。
- 生产产物共 283,342,919 bytes（34 个文件），包含一份 ORT WASM 和仓库 1 的 INT8 / FP32 固定模型分片；浏览器仍只按所选模型按需下载。
- 稳定 INT8 分片源实测返回 `206 Partial Content`、正确 `Content-Range` 和 CORS 头。
- Hugging Face 固定 revision 对 Pages Origin 返回允许跨域的响应，并声明正确模型总长度。

## Gemini 专用还原回归

浏览器实现与 WatermarkBatchLite Mac 版使用相同的 48px / 96px Alpha 模板、候选位置、相关性阈值、反向 Alpha 质量门槛和 LaMa 回退掩膜。

- 24 张 Gemini 测试图逐张比较：识别状态、处理方法、坐标、模板尺寸和 Alpha 增益全部一致；19 个 LaMa 回退掩膜及 1 个反向 Alpha 结果块的 SHA-256 也逐字节一致，差异 0。
- 24 张中 19 张进入 LaMa 局部修复、1 张通过专用反向 Alpha 直接还原、4 张保持原图。
- `clean_gemini_sample_2.png` 原先虽然通过轮廓相关性阈值，但反向 Alpha 残差为 0.965；新增残差门槛后不再送入 LaMa，也不再改动右下角像素。回归脚本会强制所有 `clean_` 样本为 `not-found`，避免把误判写进基线后“测试仍通过”。
- 现有测试集内 74 张浏览器可解码的非 Gemini 图片全部通过负样本检查，Gemini 误判 0。
- 浏览器执行异常 0；模板解码后合计 11,520 bytes，没有增加模型下载。

## 默认下载源回归

根据 iPhone 17 Pro 实机反馈，默认顺序调整为 jsDelivr → Hugging Face → 稳定版 Pages，与稳定版网站保持一致。

- jsDelivr 固定在稳定仓库提交 `b7cb12e5a1b74a2cf66372f90375683e567035a3`。
- 16MB INT8 第一分片整段下载实测约 2 秒，下载文件与仓库原件 SHA-256 一致。
- jsDelivr 返回的 `Content-Range` 总长度不可靠，因此代码只允许它整段下载；Hugging Face 和 Pages 负责严格 Range 续传。
- 全新 Chrome 缓存执行 INT8 冷启动，资源记录只出现 `cdn.jsdelivr.net`，模型下载、校验、推理全部成功，总耗时 11.0 秒，控制台错误 0。
- 从 v0.2.1 起，页面右上角显示版本和构建日期，二者由 `package.json` 与构建时间自动注入。
- v0.2.1 正式 Pages 使用全新浏览器配置冷启动：页面显示 `v0.2.1 · 2026.09.20`，4 个 INT8 分片全部来自 `cdn.jsdelivr.net`，真实豆包图处理成功，0 失败、控制台错误 0，总耗时 11.4 秒。

## 20 张真实批量回归

测试集：`/Users/kunlun/Downloads/水印测试集`

从豆包、即梦、千问、智谱清言、元宝/文心和小红书各组轮询抽取 20 张可由 Chrome 解码的 PNG、JPEG、WebP：

| 项目 | 结果 |
| --- | ---: |
| 入队 | 20 |
| 识别并修复 | 17 |
| 未识别，保持原图 | 3 |
| 失败 | 0 |
| 续跑总耗时 | 116.5 秒 |
| 浏览器控制台错误 | 0 |

在第 3 张处理中强制刷新页面：刷新前已有 2 张结果；刷新后自动恢复 20 张任务和 2 张结果，剩余 18 张继续处理并全部结束。

上述批次在加入 Gemini 支持后重新执行，17 张识别和 3 张保持原图的结果与修改前一致。

## 20 张 Gemini 真实批量回归

从 24 张 Gemini 样本中按文件名选取前 20 张，完整经过选择、识别、专用还原或 LaMa 回退、结果保存和刷新续跑：

| 项目 | 结果 |
| --- | ---: |
| 入队 | 20 |
| 识别并处理 | 17 |
| 未识别，保持原图 | 3 |
| 失败 | 0 |
| 续跑总耗时 | 218.3 秒 |
| 浏览器控制台错误 | 0 |

同样在第 3 张处理中强制刷新页面，刷新前 2 张结果和全部 20 张原图均恢复，剩余 18 张继续处理完成。

ZIP 回归从 IndexedDB 按需读取结果并生成 20 个条目（123,696,749 bytes）；Python `zipfile` 的 CRC 检查无错误，20 个文件名均带 UTF-8 标志。

批次全部完成后再次刷新：20 张任务和 20 个结果均恢复，等待 3 秒后模型分片网络请求为 0，状态保持“已恢复 20 张图片”。这验证了完整结果不常驻内存，以及已完成批次不会无条件预热模型。

## FP32 回归

同一 Chrome 环境用一张豆包样图执行 FP32：

- 模型下载、校验、Worker 初始化和推理全部完成。
- 1 张识别并修复，0 失败，浏览器控制台错误 0。
- 首次完整流程 50.4 秒，其中包含首次 198MB 模型准备。

## Pages Worker 回归

首次上线验收发现，`coi-serviceworker` 控制页面后，单独的 module Worker 请求会在部分 Chromium 环境停在加载状态。本版把已打包的 Worker 内联进主 JS，以 Blob Worker 启动；线上不再依赖第二次 Worker 脚本网络请求。

修复后用全新 Chrome 配置从正式 Pages 地址冷启动：INT8 模型下载、SHA-256 校验、Worker 初始化和豆包样图推理全部完成，1 张成功、0 失败、控制台错误 0，总耗时 17.1 秒。

v0.4.0 发布后再次用全新浏览器配置验证正式 Pages：页面版本、Gemini 文案、“处理结果”和手机顶部留白均正确；Gemini 反向 Alpha 样图 2.9 秒完成，LaMa 回退样图 8.2 秒完成，均为 1 张成功、0 失败、控制台异常 0。回退样图的模型分片只来自 `cdn.jsdelivr.net`。

## v0.4.4 审计问题回归

GPT-6 Astra 对 `ebb872dd954b4f74768450efb270279164121dc3` 的只读审计确认 2 项 P1、2 项 P2。修复后在本地 `dist/` 生产预览中执行真实 Chrome 边界检查：

- 先完成 1 张，再追加 40 张时，追加被整体拒绝；界面仍为 1 个任务、1 个结果，IndexedDB 仍为 1 个原图任务、1 个结果，结果 SHA-256 未变化。
- 首张损坏、第二张有效时，处理后为 2 个任务、1 个有效结果；刷新后仍恢复 2 个任务和该结果，两个对象仓库记录均未被整批清空。
- 128 × 128 合成无水印 JPEG 被判定为“保持原图”后，输入与 IndexedDB 结果均为 7,410 bytes，SHA-256 同为 `928063661b936f05a48307d48cd2cabdc1d878c17e35bd32830ba3324bb4d063`。
- 生产入口固定为 `assets/app.js` 和 `assets/app.css`，两者都带 14 位构建查询参数。审计证据中的旧入口 `assets/index-BR3uS04e.js` 及正式 v0.4.3 入口 `assets/index-LGdPcOC7.js` 都作为迁移桥保留，并已分别用旧 HTML 夹具验证可转入当前稳定入口；后续发布不再因哈希入口被删除而 404。
- 修复后的生产包重新跑完 20 张混合测试集：处理中已有 2 个结果时刷新，20 张任务和 2 个结果全部恢复；最终 17 张修复、3 张保持原图、0 失败、控制台错误 0，用时 111.2 秒。全部完成后再次刷新恢复 20/20，等待 3 秒模型请求为 0。
- 恢复后生成的 ZIP 共 20 个条目，CRC 全部通过且文件名均带 UTF-8 标志；其中 3 个“原图” WebP 与测试集输入逐字节一致。

## 手机尺寸检查

在 402 × 874、3x 的 iPhone 17 Pro CSS 视口与 iOS 27 Safari User-Agent 下检查首页：

- 页面显示 `v0.4.4 · 2026.09.21`，并启用 Liquid Glass 顶栏留白；主界面在安全区下按 eyebrow 实际行盒额外下移 18.5px，横屏保留紧凑布局。UI 自动检查从 `package.json` 读取 semver，只校验日期格式，不再因第二天构建而假失败。
- 顶部和底部均显示 Gemini 支持说明；结果区标题为“处理结果”。
- 主界面只显示默认 INT8 62MB；FP32 收在“模型信息”折叠区，说明为“画质基准，手机端不推荐，无明显视觉提升。”
- 不再显示推理线程提示；未选图时指标区隐藏，双预览 canvas 收起，只保留标题条。
- 选择真实图片后预览正常展开；处理完成后状态指标严格只有“水印类型”和“总耗时”。
- 测试集真实图片完成 INT8 推理，首次冷缓存 13.0 秒、再次使用缓存 4.9 秒，均为 1 张成功、0 失败、浏览器异常 0；冷缓存模型分片只来自 `cdn.jsdelivr.net`。

## 仍需实机确认

- iPhone 15 Pro iOS 18 最新版和 iPhone 17 Safari 的实际 20 张耗时、内存峰值与后台回收行为。
- iOS 原生 HEIC 解码，以及“存入相册”的系统分享面板交互。
- 低电量模式下 Wake Lock 被系统拒绝后的长批次表现。

这些项目需要真实 iPhone，桌面 Chrome 无法替代。本版在浏览器能够恢复本地站点数据的前提下，已经覆盖 Safari 重载后的任务和结果恢复。
